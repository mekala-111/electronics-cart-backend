# Loyalty Program

`LoyaltyAccount`, `LoyaltyTransaction`, `RewardRule`.  
Earn eligibility: RuleEngine on tier + min order (constructed conditions).  
Redeem under lock. Events: `loyalty.points.earned` / `redeemed`.
