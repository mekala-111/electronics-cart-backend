# Recommendation Engine

No ML. Uses `ProductRecommendation` pairs + optional condition JSON (stored via audit).  
Also: recently viewed (`RecentlyViewedProduct`).  
Event: `recommendation.generated`.
